package Chat_router

import (
	"github.com/gofiber/fiber/v2"
	"github.com/gofiber/fiber/v2/middleware/session"
)

func ChatMediaRouter(app fiber.Router, store session.Store) {
	//app.Post("/create-comment", create_comment(store))
}